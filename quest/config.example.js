// Kopieer dit bestand naar config.js (staat in .gitignore) en vul binId + accessKey in.
// Gebruik NOOIT de Master Key in de browser — alleen de Access Key met read+update.
// Waarden vind je in .env na setup, of op https://jsonbin.io/app/api-keys
window.TOPO_QUEST_JSONBIN = {
  binId: '',
  accessKey: ''
};
